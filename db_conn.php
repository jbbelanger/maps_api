<?php
$servername = "hp187.hostpapa.com";
$username = "jbeno958_admin";
$password = "vibe2192";
$dbname = "jbeno958_election";

/*$conn = new mysqli($servername, $username, $password, $dbname);
$conn->set_charset('utf8mb4');

$sql = "select
id,
regi_nom,
regi_geometry
from region";
$row;
$result = $conn->query($sql);
$regions_geometry_array = [];
if ($result->num_rows > 0) {
  while ($row = $result->fetch_assoc()) {
    array_push($regions_geometry_array, ["id"=>$row["id"],"nom"=>$row["regi_nom"],"geometry"=>$row["regi_geometry"]]);
  }
} else {
  echo "<pre><code>\$result->num_rows < 0</code></pre> ou quelque chose d'autre";
}

//var_dump($regions_geometry_array[2]["nom"]);

$conn->close();*/

?>
